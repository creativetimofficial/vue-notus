version: '2.2'

services:
    femodule:
      ports:
        - 80:8080
      build:
        context: .
        dockerfile: Dockerfile
      volumes:
          - ./:/usr/src/app
          - ./.env:/usr/src/app/.env
      command: "npm run serve"
      restart: unless-stopped
